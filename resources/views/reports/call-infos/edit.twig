{% extends "layout/html" %}

{% block content %}
    <div class="container section" id="vue">
        <div class="panel">
            <div class="box" style="margin-top: 20px">
                <h4 class="title">{{ title }}</h4>
                <form action="{{ record is defined ? route('reports-call-infos.update', {'id':record.id}) : route('reports-call-infos.store')}}" method="post">
                    {% if record is defined %}
                        {{  method_field('PUT') }}
                    {% endif %}
                    {{ csrf_field() }}

                    <div class="field">
                        <label for="">Дата</label>
                        {% if record is defined %}
                            <input value="{{ record.date }}" type="text" class="input" disabled>
                        {% else %}
                            <input type="datetime-local" class="input" value="" name="date">
                        {% endif %}
                    </div>

                    <div class="field">
                        <label for="">101</label>
                        <input type="number" class="input" value="{{ record.count_101|default('') }}" name="count_101">
                    </div>

                    <div class="field">
                        <label for="">102</label>
                        <input type="number" class="input" value="{{ record.count_102|default('') }}" name="count_102">
                    </div>

                    <div class="field">
                        <label for="">103</label>
                        <input type="number" class="input" value="{{ record.count_103|default('') }}" name="count_103">
                    </div>

                    <div class="field">
                        <label for="">109</label>
                        <input type="number" class="input" value="{{ record.count_109|default('') }}" name="count_109">
                    </div>

                    <div class="field">
                        <label for="">112</label>
                        <input type="number" class="input" value="{{ record.count_112|default('') }}" name="count_112">
                    </div>

                    <div class="field">
                        <label for="">Информационно – справочного характера</label>
                        <input type="number" class="input" value="{{ record.count_info|default('') }}" name="count_info">
                    </div>

                    <div class="field">
                        <label for="">Прочее (проверка сотовых телефонов, шалость детей и т.д.)</label>
                        <input type="number" class="input" value="{{ record.count_other|default('') }}" name="count_other">
                    </div>

                    <div class="field">
                        <label for="">По вопросам реагирования на ЧС природного и техногенного характера</label>
                        <input type="number" class="input" value="{{ record.count_emergency|default('') }}" name="count_emergency">
                    </div>

                    <div class="field">
                        <label for="">Примечание</label>
                        <textarea class="textarea" name="note" id="note">{{ record.note|default('') }}</textarea>
                    </div>


                    <div class="buttons has-text-right is-grouped is-right" style="">
                        <button type="submit" class="button is-success"><i class="fas fa-check"></i>&nbsp;Сохранить</button>
                        <a href="{{ url('/reports/call-infos/') }}" class="button is-light">Отменить</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock content %}
