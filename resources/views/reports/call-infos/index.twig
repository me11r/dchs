{% extends "layout/html" %}
{% block content %}
    <div class="container section">
            <div class="level">
                <div class="level-left">
                    <h4 style="" class="title">Информация по звонкам</h4>
                </div>
                <div class="level-right has-text-right">
                    <a href="{{ route('reports-call-infos.create') }}" class="button is-basic is-main"><i class="fas fa-plus"></i>&nbsp;Создать запись</a>
                </div>
                <div class="level-right has-text-right">
                    <a href="{{ route('reports-call-infos.show') }}" class="button is-dark"><i class="fas fa-glasses"></i>&nbsp;Отчет за период</a>
                </div>
            </div>
        <div class="panel">
            <table class="table is-striped is-narrow is-hoverable is-selectable">
                <thead>
                    <tr>
                        <th>101</th>
                        <th>102</th>
                        <th>103</th>
                        <th>109</th>
                        <th>112</th>
                        <th>Дата</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    {% for item in items %}
                        <tr>
                            <td>{{ item.count_101 }}</td>
                            <td>{{ item.count_102 }}</td>
                            <td>{{ item.count_103 }}</td>
                            <td>{{ item.count_109 }}</td>
                            <td>{{ item.count_112 }}</td>
                            <td>{{ item.date|date('d.m.Y') }}</td>
                            <td>
                                <div class="field is-grouped">
                                <a class="button is-basic is-small" href="{{ route('reports-call-infos.edit', {'id':item.id}) }}">
                                    <i class="fas fa-edit"></i>&nbsp;Редактировать</a>

                                <form method="post" action="{{ route('reports-call-infos.delete', {'id':item.id}) }}">
                                    {{ csrf_field() }}
                                    {{  method_field('DELETE') }}
                                    <button onclick="return confirm('Удалить запись?');" class="button is-danger is-small">
                                        <i class="fas fa-crosshairs"></i>&nbsp;Удалить</button>
                                </form>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr><h3 class="is-danger">Нет данных в таблице</h3></tr>
                    {% endfor %}
                </tbody>

            </table>
            {% include "_templates/pagination" with {'items': items, 'per_page': per_page} %}

        </div>
    </div>
{% endblock content %}
