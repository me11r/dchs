{% extends "layout/html" %}
{% block content %}
    <div class="container section">
        <div class="level">
            <div class="level-left">
                <h3 class="title">Спасоперации</h3>
            </div>
            <div class="level-right">
                <a href="{{ url('/formation/savers/event/' ~ parent.id ~'/0') }}" class="button is-main is-basic"><i class="fas fa-plus"></i>&nbsp; Создать запись</a>
            </div>
        </div>
        <div>
            <table class="table is-narrow is-hoverable is-striped is-fullwidth">
                <thead>
                <tr>
                    <th>Дата</th>
                    <th>Описание</th>
                    <th>&nbsp;</th>
                </tr>
                </thead>
                <tbody>
                {% for report in reports %}
                    <tr>
                        <td>{{ report.event_date|default('') }}</td>
                        <td>{{ report.comment|default('') }}</td>
                        <td class="has-text-right"><a href="{{ url('/formation/savers/event/' ~ parent.id ~ '/' ~ report.id) }}" class="button is-basic is-small">
                                <i class="fas fa-edit"></i>&nbsp;Изменить
                            </a></td>
                    </tr>
                {% else %}
                    <tr>
                        <th colspan="3" class="has-text-centered"><h4 class="subtitle">Нет данных о спасоперациях</h4></th>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
{% endblock content %}
