{% extends "layout/html" %}
{% block content %}
    <div class="container section">
        <div class="panel">
            <div class="level">
                <div class="level-left"><h4 style="" class="title">Список ЛС</h4></div>
                <div class="level-right has-text-right">
                    <a href="{{ route('staff.create') }}" class="button is-basic is-main"><i class="fas fa-plus"></i>&nbsp;Создать запись</a>
                </div>
            </div>


            <table class="table is-narrow is-hoverable is-selectable is-striped">
                <thead>
                <tr>
                    <th>Ф.И.О.</th>
                    <th>Звание</th>
                    <th>Должность</th>
                    <th>ПЧ</th>
                    <th></th>
                </tr>
                </thead>
                {% for item in items %}
                    <tr>
                        <td><i class="fas fa-list-ol"></i>&nbsp;{{ item.name }}</td>
                        <td>{{ item.rank }}</td>
                        <td>{{ item.position }}</td>
                        <td>{{ item.department.name ?? '' }}</td>
                        <td>
                            <div class="field is-grouped">
                            <a class="button is-basic is-small" href="{{ route('staff.edit', {'id':item.id}) }}">
                                <i class="fas fa-edit"></i>&nbsp;Редактировать</a>

                            <form method="post" action="{{ route('staff.destroy', {'id':item.id}) }}">
                                {{ csrf_field() }}
                                {{  method_field('DELETE') }}
                                <button onclick="return confirm('Удалить запись?');" class="button is-danger is-small">
                                    <i class="fas fa-trash"></i>&nbsp;Удалить</button>
                            </form>
                            </div>
                        </td>
                    </tr>
                {% else %}
                    <li><h3 class="is-danger">Нет ЛС</h3></li>
                {% endfor %}
            </table>
            {% include "_templates/pagination" with {'items': items, 'per_page': per_page} %}

        </div>
    </div>
{% endblock content %}
