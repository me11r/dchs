{% extends "layout/html" %}
{% block content %}
    <div class="container section">
    <div class="level">
        <div class="level-left">
        <h4 class="title"
            style="padding: 3px 15px">Оперативная информация
        </h4>
        </div>
        <div class="level-right">
            <a href="{{ route('emergency-situation.create') }}" class="button is-main is-basic"
               >
                <i class="fas fa-plus"></i>&nbsp;Добавить
            </a>
        </div>
    </div>
    <div class="panel">
        <table class="table is-narrow is-hoverable is-fullwidth is-striped is-small">
            <thead>
            <tr>
                <th>Дата и время</th>
                <th>Служба взаимодействия</th>
                <th>Место происшествия</th>
                <th>Характер и масштаб</th>
                <th>&nbsp;</th>
            </tr>
            </thead>
            {% for item in items %}
                <tr>
                    <td>{{ item.date|date('Y-m-d') }} {{ item.time }}</td>
                    <td>{{ item.user.service_type.name|default('') }}</td>
                    <td>{{ item.location }}</td>
                    <td>{{ item.size }}</td>
                    <td class="has-text-right">
                        <a href="/emergency-situation/{{ item.id }}/download"
                           class="button is-small is-success">
                            <i class="fas fa-download"></i>&nbsp;
                        </a>
                        <a href="{{ route('emergency-situation.show', item.id) }}"
                           class="button is-small is-info">
                            <i class="fas fa-eye"></i>&nbsp;
                            Просмотр
                        </a>
                        <a href="{{ route('emergency-situation.edit', item.id) }}"
                           class="button is-small is-basic">
                            <i class="fas fa-pencil-alt"></i>&nbsp;
                            Редактировать
                        </a>
                        <form style="display: inline;" onsubmit="return confirm('Вы действительно хотите удалить эту запись?');"
                              method="POST"
                              action="{{ route('emergency-situation.destroy', item.id) }}">
                            {{ csrf_field() }}
                            <input type="hidden" name="_method" value="DELETE">
                            <button title="Удалить"
                                    class="button is-small is-danger">
                                <i class="fas fa-trash"></i>
                            </button>
                        </form>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td class="is-centered"><h1 class="title">Нет записей</h1></td>
                </tr>
            {% endfor %}
        </table>
    </div>
    </div>
{% endblock content %}
