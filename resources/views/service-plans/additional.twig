{% extends "layout/html" %}
{% block content %}
    <div class="section container" id="vue">
        <h4 class="title"><a href="/service-plans/{{ record.service_type_id }}/{{ record.id }}/show">Путевой лист №{{ record.id }}: </a>Информация от {{ record.service_type.name }}</h4>
        <service-plans-additional
            :record="{{ record }}"
        ></service-plans-additional>
    </div>
{% endblock content %}