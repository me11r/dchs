{% extends "layout/html" %}
{% block content %}
    <div id="vue">
        <section>
            <b-tabs position="is-centered" class="block">
                <b-tab-item label="Сведения о чрезвычайных ситуациях">
                    <main-report :current_year="{{ current_year }}" :previous_year="{{ previous_year }}" :card112_results="{{ results112 }}" :card101_results="{{ results }}"></main-report>
                </b-tab-item>
                <b-tab-item label="Статус">
                    <report-forces></report-forces>
                </b-tab-item>
            </b-tabs>
        </section>
    </div>
{% endblock content %}